# artikBroker runtime — pinned to the known-good local venv (Python 3.13)
# The artik_engine package is installed separately in the Dockerfile (--no-deps),
# so numpy/pandas/yfinance are pinned here as its real dependencies.
fastapi==0.124.4
uvicorn[standard]==0.38.0
python-multipart==0.0.32
yfinance==1.3.0
pandas==2.3.3
numpy==2.4.0
anthropic==0.84.0
openai==2.14.0
boto3==1.42.14
cryptography==46.0.3
