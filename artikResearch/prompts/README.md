# Prompts

Agent system prompts live in code with each agent (`backend/app/agents/*.py`) so they are
versioned and testable alongside the schema they drive. This folder holds any exported /
shareable prompt variants and user-authored templates. See `backend/app/agents/__init__.py`
for the agent catalog.
